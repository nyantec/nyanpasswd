<main>
  <p>Your new password is: <code><%= password %></code></p>

  <p>Please copy it somewhere safe because you will never see it again!</p>

  <a href="<%= prevlink.unwrap_or_else(|| "/".to_owned()) %>">Return to the main page</a>
</main>